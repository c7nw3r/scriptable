apply plugin: "antlr"

repositories {
    mavenCentral()
}

dependencies {
    antlr "org.antlr:antlr4:4.9.3"
}

generateGrammarSource {
    outputDirectory = file("${projectDir}/scriptable/antlr")
    arguments += ["-visitor", "-Dlanguage=Python3"]
}
